spring:
  datasource:
    url: jdbc:h2:mem:parking_garage;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

simulator:
  base-url: http://localhost:3000
